package PF_ForwardWithMutualExclusiveAttr;

import org.apache.beehive.netui.pageflow.*;
import org.apache.beehive.netui.pageflow.annotations.Jpf;

/**
 *******************************************************************************
 *
 * @desc: This PageFlowController file declares a jpf:forward annotations in Forward
 * method "page1" with both the "path" and "returnAction" attributes.  These two
 * attributes are mutually exclusive.
 *
 * @result: The compiler should catch this and issue an error.
 *
 *
 ******************************************************************************/
    @Jpf.Controller(
        nested = true)
public class Controller extends PageFlowController
    {
    @Jpf.Action(
        forwards = {
            @Jpf.Forward(
                name = "begin",
                path = "Begin.jsp") 
        })
    protected Forward begin()
        {
        return new Forward("begin");
        }

    @Jpf.Action(
        forwards = {
            @Jpf.Forward(
                name = "page1",
                path = "Page1.jsp",
                returnAction = "createTest") 
        })
    protected Forward page1()
        {
        return new Forward("page1");
        }
    }
