package PF_MissingBegin;

import org.apache.beehive.netui.pageflow.*;
import org.apache.beehive.netui.pageflow.annotations.Jpf;

/**
 *******************************************************************************
 *
 * @desc: This PageFlowController file does not contain the required "begin" method and
 * annotation.
 *
 * @result: The compiler should catch this omission and issue an error that the "begin"
 * method is missing.
 *
 ******************************************************************************/
@Jpf.Controller()
public class Controller extends PageFlowController
    {
    @Jpf.Action(
        forwards = {
            @Jpf.Forward(
                name = "pg1",
                path = "Pg1.jsp")
        })
    protected Forward page1()
        {
        return new Forward("pg1");
        }
    }
